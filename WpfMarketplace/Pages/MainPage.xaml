<Page x:Class="WpfMarketplace.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfMarketplace.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Background="#EE6C4D" HorizontalAlignment="Stretch" Orientation="Horizontal">
            <Button x:Name="ButtonBack" Click="ButtonBack_Click" Height="30" Width="50" Padding="0">
                <Button.Content>
                    <TextBlock FontSize="10">
                        Назад
                    </TextBlock>
                </Button.Content>
            </Button>
            <Label VerticalAlignment="Center" Foreground="White">
                <TextBlock FontSize="24">
                    Маркетплейс
                </TextBlock>
            </Label>
        </StackPanel>
        <TextBlock Margin="20 0 0 0" x:Name="TitleTb" VerticalAlignment="Center" FontSize="18" HorizontalAlignment="Center" Foreground="White">
            Заголовок
        </TextBlock>
        <Button HorizontalAlignment="Right" Width="100" Height="30" Padding="0" x:Name="CreateProudctButton" Click="CreateProudctButton_Click">
            <Button.Content>
                <TextBlock FontSize="10">                    
                    Создать товар
                </TextBlock>
            </Button.Content>
        </Button>
        <Frame x:Name="pageMainFrame" Grid.Row="1" Grid.Column="1"></Frame>
    </Grid>
</Page>
